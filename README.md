# Servidor

Esta branch contêm o código do servidor. O servidor é responsável por gerenciar todas as informações e armazená-las usando SQLite.

Uma API é disponibilizada através do protocolo HTTP para que o website e aplicativo possam acessar tais informações.

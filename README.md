# WebSite

Esta branch contém o código do website do projeto. O website é responsável por fornecer a interface para gerenciar o sistema de impressão e também o de rastreio.

## Rodar

Com o Node.js, inicie o servidor com o comando:

```bash
node server.js
```

